USAGE:
   mason schema -action [OPTIONS]

   Create and update schemas within a project

OPTIONS:
    create -path {path_to_schema} Create a new schema
    update -path {path_to_schema} Update an existing schema
    get -id {schema_id} Get a schema by id
    get -all true Get all schemas
    delete -id {schema_id} Delete a schema
    
